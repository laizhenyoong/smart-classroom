{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Backend for Smart Classroom",
  "scripts": {
    "dev": "node --env-file=.env --watch src/server.js",
    "start": "node --env-file=.env src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
